{
  "name": "fenny-ts",
  "version": "0.0.1",
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "esbuild": "^0.14.1",
    "jest": "^27.4.3",
    "ts-jest": "^27.0.7",
    "typescript": "^4.5.2"
  },
  "scripts": {
    "build:dts": "tsc",
    "build:bundle": "node ./scripts/build.js",
    "build": "yarn build:bundle && yarn build:dts",
    "test": "jest"
  }
}
